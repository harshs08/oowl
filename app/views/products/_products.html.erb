    <div id="shelf-wrapper">
      <div id="container" class="shelf-img">
        <div class="row">
          <% @products.dress.each_with_index do |product,idx| %>
          <div class="img">
            <%= image_tag("#{product.image.url(:medium)}", :class => "draggable", :data => { :height => product.height, :width => product.width, :price => product.price, :name => product.name, :pos => product.id }) %>
            </div>
          <% end %>
        </div>
        <div class="row" id="furniture">

          <% @products.furniture.each_with_index do |product, idx| %>
            <div class="img">
            <%= image_tag("#{product.image.url(:medium)}", :class => "draggable", :data => { :height => product.height, :width => product.width, :price => product.price, :name => product.name, :pos => product.id }) %>
            </div>
          <% end %>
        </div>
        <div class="row">
          <% @products.miscel.each_with_index do |product, idx| %>
            <div class="img">
            <%= image_tag("#{product.image.url(:original)}", :class => "draggable", :data => { :height => product.height, :width => product.width, :price => product.price, :name => product.name, :pos => product.id }) %>
            <% %>
            </div>
          <% end %>
        </div>
        <div class="row">
          <% @products.others.each_with_index do |product, idx| %>
            <div class="img">
            <%= image_tag("#{product.image.url(:original)}", :class => "draggable", :data => { :height => product.height, :width => product.width, :price => product.price, :name => product.name, :pos => product.id }) %>
            <% %>
            </div>
          <% end %>
        </div>
      </div>
    </div> 